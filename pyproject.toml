[project]
name = "autocode"
version = "0.1.0"
description = "Standalone 24/7 autonomous coding chat driver"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
autocode = "autocode.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

